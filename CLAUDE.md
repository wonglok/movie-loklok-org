@AGENTS.md

MUST USE zustand for state management

MUST add a modal confirm for removing items

Must use object `_id` or `id` or `key` to find the object in array.
Must not use `array index` to get the object in array.

use autosave when suitable

must always save blob files to workspace's correct folder
must always load blob files from workspace's correct folder
